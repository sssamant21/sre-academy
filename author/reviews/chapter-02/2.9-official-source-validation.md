# Chapter 2.9 Official Source Validation

## Section

2.9 Cluster Upgrade and Version Skew Architecture

## Review Status

Passed for author draft integration.

## Scope Reviewed

This validation reviewed Section 2.9 for alignment with upstream Kubernetes documentation on version skew, supported releases, kubeadm upgrade
flow, safe node draining, `kubectl drain`, and PodDisruptionBudgets.

## Official Sources Reviewed

- [Kubernetes Version Skew Policy](https://kubernetes.io/releases/version-skew-policy/)
- [Kubernetes Releases](https://kubernetes.io/releases/)
- [Upgrading kubeadm Clusters](https://kubernetes.io/docs/tasks/administer-cluster/kubeadm/kubeadm-upgrade/)
- [Safely Drain a Node](https://kubernetes.io/docs/tasks/administer-cluster/safely-drain-node/)
- [kubectl drain](https://kubernetes.io/docs/reference/kubectl/generated/kubectl_drain/)
- [Disruptions](https://kubernetes.io/docs/concepts/workloads/pods/disruptions/)

## Claim Validation Matrix

| Draft claim area | Validation result | Notes |
| --- | --- | --- |
| Kubernetes maintains release branches for the most recent three minor releases. | Passed | Aligned with Kubernetes releases and version skew policy documentation. |
| Kubernetes 1.19 and newer receive approximately one year of patch support. | Passed | Aligned with Kubernetes releases documentation. |
| HA `kube-apiserver` instances must remain within one minor version of each other. | Passed | Aligned with version skew policy. |
| kubelet must not be newer than kube-apiserver and can lag within the supported skew window. | Passed | Aligned with version skew policy. |
| kube-proxy must not be newer than kube-apiserver and can lag within the supported skew window. | Passed | Aligned with version skew policy. |
| kube-controller-manager, kube-scheduler, and cloud-controller-manager are expected to match kube-apiserver but can be one minor behind during upgrade. | Passed | Aligned with version skew policy. |
| kubectl is supported within one minor version older or newer than kube-apiserver. | Passed | Aligned with version skew policy. |
| kubeadm upgrade flow upgrades a primary control-plane node, additional control-plane nodes, and then worker nodes. | Passed | Aligned with kubeadm upgrade documentation. |
| Skipping Kubernetes minor versions during upgrade is unsupported. | Passed | Aligned with kubeadm upgrade and version skew policy documentation. |
| Minor kubelet upgrades require draining nodes first. | Passed | Aligned with version skew policy and kubeadm upgrade guidance. |
| `kubectl drain` uses eviction where supported and respects PodDisruptionBudgets. | Passed | Aligned with `kubectl drain`, safe drain, and disruption documentation. |
| PDBs limit voluntary disruptions but do not guarantee spare capacity by themselves. | Passed | Aligned with disruptions documentation and SRE interpretation. |
| Admission webhooks require compatibility review because they are in the API write path. | Passed | Aligned with version skew policy guidance for webhooks during API server upgrades. |

## Markdown and Navigation Checks

- Front matter present.
- Single H1 used for the section title.
- Heading hierarchy reviewed.
- Mermaid fences use `mermaid`.
- YAML examples use `yaml`.
- Shell examples use `bash`.
- No internal relative links were introduced.
- MkDocs navigation update required for the published wrapper page.

## Notes

The section intentionally distinguishes upstream Kubernetes version-skew guarantees from managed Kubernetes or distribution-specific upgrade
rules. Managed platforms can be stricter than upstream Kubernetes and must be checked separately during production planning.
