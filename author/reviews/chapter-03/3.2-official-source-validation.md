# Chapter 3.2 Official Source Validation

## Section

3.2 Pods, Containers, and Init Containers

## Review Status

Passed for author draft integration.

## Scope Reviewed

This validation reviewed Section 3.2 for alignment with upstream Kubernetes documentation on Pods, Pod lifecycle, container states, init
containers, sidecar containers, restart policy, probes, and production troubleshooting interpretation.

## Official Sources Reviewed

- [Pods](https://kubernetes.io/docs/concepts/workloads/pods/)
- [Pod Lifecycle](https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/)
- [Init Containers](https://kubernetes.io/docs/concepts/workloads/pods/init-containers/)
- [Sidecar Containers](https://kubernetes.io/docs/concepts/workloads/pods/sidecar-containers/)
- [Liveness, Readiness, and Startup Probes](https://kubernetes.io/docs/concepts/workloads/pods/probes/)

## Claim Validation Matrix

| Draft claim area | Validation result | Notes |
| --- | --- | --- |
| A Pod is the smallest deployable unit in Kubernetes and provides shared context for one or more containers. | Passed | Aligned with Pods documentation. |
| Containers in the same Pod share network namespace and can communicate over localhost. | Passed | Aligned with Pods documentation. |
| Init containers run before app containers and each must complete successfully before the next starts. | Passed | Aligned with Init Containers documentation. |
| A Pod cannot become Ready until init containers have succeeded. | Passed | Aligned with Init Containers documentation. |
| Init container resource requests affect effective Pod resource requirements and scheduling. | Passed | Aligned with Init Containers resource sharing documentation. |
| Native sidecars are specified through `initContainers` with container-level `restartPolicy: Always`. | Passed | Aligned with Pod lifecycle and sidecar container documentation. |
| Pod phase is distinct from friendly `kubectl` status strings such as `CrashLoopBackOff`. | Passed | Aligned with Pod Lifecycle documentation. |
| Container states include Waiting, Running, and Terminated. | Passed | Aligned with Pod Lifecycle documentation. |
| Pod-level restart policies include Always, OnFailure, and Never. | Passed | Aligned with Pod Lifecycle documentation. |
| Startup, readiness, and liveness probes have different purposes and operational consequences. | Passed | Aligned with probes documentation. |
| Readiness failures remove matching Service endpoints from EndpointSlices. | Passed | Aligned with probes documentation. |
| Liveness probes must be used carefully because incorrect probes can worsen incidents. | Passed | Aligned with probes documentation caution. |

## Markdown and Navigation Checks

- Front matter present.
- Single H1 used for the section title.
- Heading hierarchy reviewed.
- Mermaid fences use `mermaid`.
- YAML examples use `yaml`.
- Shell examples use `bash`.
- No internal relative links were introduced.
- MkDocs navigation update required for the published wrapper page.

## Notes

The section intentionally separates Pod phase, container state, and `kubectl` display status to avoid a common troubleshooting mistake. It also
frames liveness probes as a recovery mechanism for local unrecoverable failures, not a general dependency health check.
