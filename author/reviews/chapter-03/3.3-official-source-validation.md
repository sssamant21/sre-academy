# Chapter 3.3 Official Source Validation

## Section

3.3 Deployments and ReplicaSets

## Review Status

Passed for author draft integration.

## Scope Reviewed

This validation reviewed Section 3.3 for alignment with upstream Kubernetes documentation on Deployments, ReplicaSets, rolling update behavior,
rollout history, rollback, rollout pause and resume, Deployment strategy fields, progress deadlines, and production troubleshooting interpretation.

## Official Sources Reviewed

- [Deployments](https://kubernetes.io/docs/concepts/workloads/controllers/deployment/)
- [ReplicaSet](https://kubernetes.io/docs/concepts/workloads/controllers/replicaset/)
- [Update a Deployment Without Downtime](https://kubernetes.io/docs/tasks/run-application/update-deployment-rolling/)
- [Deployment API reference](https://kubernetes.io/docs/reference/kubernetes-api/apps/deployment-v1/)
- [kubectl rollout](https://kubernetes.io/docs/reference/kubectl/generated/kubectl_rollout/)

## Claim Validation Matrix

| Draft claim area | Validation result | Notes |
| --- | --- | --- |
| A Deployment provides declarative updates for Pods and ReplicaSets. | Passed | Aligned with Deployments documentation. |
| A Deployment creates and manages ReplicaSets for rollout revisions. | Passed | Aligned with Deployments documentation. |
| A ReplicaSet maintains a stable set of replica Pods. | Passed | Aligned with ReplicaSet documentation. |
| A ReplicaSet has selector, replica count, and Pod template fields. | Passed | Aligned with ReplicaSet documentation. |
| ReplicaSets own Pods through owner references. | Passed | Aligned with ReplicaSet ownership behavior. |
| ReplicaSets can acquire matching Pods without a controller owner. | Passed | Aligned with ReplicaSet documentation. |
| Engineers should normally use Deployments instead of directly manipulating ReplicaSets. | Passed | Aligned with ReplicaSet documentation guidance. |
| Deployment strategy supports `RollingUpdate` and `Recreate`. | Passed | Aligned with Deployment API reference. |
| `RollingUpdate` is the default Deployment strategy. | Passed | Aligned with Deployment API reference. |
| `maxSurge` defaults to 25 percent and percentage values round up. | Passed | Aligned with Deployment API reference. |
| `maxUnavailable` defaults to 25 percent and percentage values round down. | Passed | Aligned with Deployment API reference. |
| `maxSurge` and `maxUnavailable` cannot both be zero. | Passed | Aligned with Deployment API reference. |
| Deployment Pod template restart policy must be `Always`. | Passed | Aligned with Deployment API reference. |
| `.spec.progressDeadlineSeconds` defaults to 600 seconds. | Passed | Aligned with Deployment rollout documentation. |
| A stalled rollout can report `ProgressDeadlineExceeded`. | Passed | Aligned with Deployment rollout documentation. |
| `kubectl rollout` supports status, history, undo, pause, and resume flows. | Passed | Aligned with kubectl rollout and Deployment task documentation. |

## Markdown and Navigation Checks

- Front matter present.
- Single H1 used for the section title.
- Heading hierarchy reviewed.
- Mermaid fences use `mermaid`.
- YAML examples use `yaml`.
- Shell examples use `bash`.
- Tables use standard Markdown pipe syntax.
- No internal relative links were introduced.
- MkDocs navigation update required for the published wrapper page.

## Notes

The section intentionally distinguishes Deployment rollout state from application health. Rollout success means Kubernetes reached the desired
controller state, but production validation still requires service-level telemetry and release-specific checks.
