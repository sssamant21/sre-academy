# Chapter 3.5 Official Source Validation

## Section

3.5 Jobs and CronJobs

## Review Status

Passed for author draft integration.

## Scope Reviewed

This validation reviewed Section 3.5 for alignment with upstream Kubernetes documentation on Jobs, CronJobs, Job retry behavior, parallelism,
completion tracking, indexed Jobs, CronJob schedules, time zones, concurrency policies, history limits, suspension, missed starts, and TTL cleanup.

## Official Sources Reviewed

- [Jobs](https://kubernetes.io/docs/concepts/workloads/controllers/job/)
- [CronJob](https://kubernetes.io/docs/concepts/workloads/controllers/cron-jobs/)
- [Automatic Cleanup for Finished Jobs](https://kubernetes.io/docs/concepts/workloads/controllers/ttlafterfinished/)
- [Running Automated Tasks with a CronJob](https://kubernetes.io/docs/tasks/job/automated-tasks-with-cron-jobs/)
- [Job API reference](https://kubernetes.io/docs/reference/kubernetes-api/batch/job-v1/)

## Claim Validation Matrix

| Draft claim area | Validation result | Notes |
| --- | --- | --- |
| A Job creates Pods and tracks successful completion of finite work. | Passed | Aligned with Job documentation. |
| A CronJob creates Jobs on a repeating schedule. | Passed | Aligned with CronJob documentation. |
| Job Pod template restart policy must be `Never` or `OnFailure`. | Passed | Aligned with Job API behavior. |
| `completions` and `parallelism` control required successes and concurrent Pods. | Passed | Aligned with Job documentation. |
| Indexed Jobs use `completionMode: Indexed` for index-based work partitioning. | Passed | Aligned with Job documentation. |
| `backoffLimit`, `backoffLimitPerIndex`, and `maxFailedIndexes` affect retry and failure handling. | Passed | Aligned with Job documentation. |
| `podFailurePolicy` supports policy-based handling of Pod failures. | Passed | Aligned with Job documentation. |
| `activeDeadlineSeconds` limits how long a Job may be active. | Passed | Aligned with Job documentation. |
| `ttlSecondsAfterFinished` provides automatic cleanup for finished Jobs. | Passed | Aligned with TTL-after-finished documentation. |
| CronJob `.spec.schedule` is required and uses cron-style syntax. | Passed | Aligned with CronJob documentation. |
| CronJob `.spec.jobTemplate` is required and defines created Jobs. | Passed | Aligned with CronJob documentation. |
| CronJob concurrency policies include `Allow`, `Forbid`, and `Replace`. | Passed | Aligned with CronJob documentation. |
| CronJob `Allow` is the default concurrency policy. | Passed | Aligned with CronJob documentation. |
| `startingDeadlineSeconds` controls delayed starts after missed schedules. | Passed | Aligned with CronJob documentation. |
| CronJob suspension prevents future Jobs from starting but does not stop Jobs already started. | Passed | Aligned with CronJob documentation. |
| CronJob successful and failed history limits default to 3 and 1. | Passed | Aligned with CronJob documentation. |
| CronJob `.spec.timeZone` is stable and should be used instead of `TZ` or `CRON_TZ` inside the schedule. | Passed | Aligned with CronJob documentation. |

## Markdown and Navigation Checks

- Front matter present.
- Single H1 used for the section title.
- Heading hierarchy reviewed.
- Mermaid fences use `mermaid`.
- YAML examples use `yaml`.
- Shell examples use `bash`.
- Cron syntax diagram uses `text`.
- Tables use standard Markdown pipe syntax.
- No internal relative links were introduced.
- MkDocs navigation update required for the published wrapper page.

## Notes

The section intentionally treats Kubernetes Job success as separate from business success. Production validation still requires checking external
artifacts, side effects, audit records, and downstream system state.
