# DNS

DNS is documented inside the Networking handbook because name resolution is a core production networking concern.

## Scope

- Records, zones, resolvers, and delegation
- TTLs, caching, propagation, and failover
- Split-horizon DNS and private zones
- Monitoring, validation, and troubleshooting
- Operational runbooks for DNS incidents

## Related pages

The legacy standalone DNS page is preserved at `books/dns/` for history, but new DNS content should live in this Networking section.
