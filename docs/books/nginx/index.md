# NGINX

The NGINX handbook will document reliable proxy, ingress, and web serving operations.

## Planned chapters

- Server blocks, upstreams, routing, and TLS
- Reverse proxy, caching, and static asset patterns
- Timeouts, retries, buffers, and rate limits
- Logging, metrics, reloads, and configuration validation
- Troubleshooting failed upstreams and traffic errors

## Starter reliability questions

1. How do we validate configuration before reload?
2. Which upstream errors require application versus proxy investigation?
3. What rollback path exists for a bad routing change?
